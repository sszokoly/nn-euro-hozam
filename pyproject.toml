[project]
name = "nn-euro-hozam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.60.0",
    "selenium>=4.33.0",
]

[project.scripts]
nn-euro-hozam = "nn_euro_hozam:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
py-modules = ["nn_euro_hozam"]

[dependency-groups]
dev = [
    "ruff>=0.15.13",
]
